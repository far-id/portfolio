```ts
/**
 * @title Personal Skills
 *.
 */

const languages = ['PHP', 'JavasScript', 'TypeScript', 'SQL'];

const framework = {
	web: ['React.js', 'Next.js', 'Laravel', 'TailwindCSS', 'Shadcn'],
	backend: ['Laravel', 'Next.js', 'Express.js'],
};

const tools = [
	'Git',
	'Docker',
	'kubernetes',
	'GCP (been a while)',
	'Vercel',
	'Linear (Trying to stay on track (but failing))',
];

export { languages, framework, tools };
```
